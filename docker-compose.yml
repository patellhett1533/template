version: '3'

services:
  front:
    build:
      context: .
      dockerfile: Dockerfile
    command: >
      sh -c "npm install && npm run dev"
    env_file:
      - ./.env
    ports: 
      - 12801:3000
    volumes:
      - .:/app
    networks:
      - common-network

networks:
  common-network:
    driver: bridge